CacaDetails.Js :
    clique sur le bouton modifier
    fait apparaître un formulaire (qui était en display none)
    les input du formulaire = valeurs du caca en cours
    quand on confirme, on actualise les valeurs ? voir avec un fetch
    
Étapes :
    Créer un formulaire dans une div en display none
